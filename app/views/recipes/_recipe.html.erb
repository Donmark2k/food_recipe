<%= link_to recipe do%>
<div id="<%= dom_id recipe %>" class="flex p-8 justify-between">
  <div class="">
    <p class="text-2xl">
    <%= recipe.name %>
  </p>
    <%= button_to 'Destroy this recipe', recipe_path(recipe), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
  </div>

  <p class="my-5 w-3/4">
    <%= recipe.description %>
  </p>
</div>
<%end%>
